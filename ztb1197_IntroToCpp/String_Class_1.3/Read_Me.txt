Tests the various String functions created for Assessment One.

Outputs test results into "test.txt" if test.txt isn't already created then the program will 
 create it automatically.

Will ask for an input of some kind at the beginning and then output what you inputted and ask if the two were the same thing.
 If the output is the same as input type "y" for yes.
 If the output is not the same as input type "n" for no.